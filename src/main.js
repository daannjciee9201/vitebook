// main.js
import './style.scss' // 確保這行在最上面
import * as bootstrap from 'bootstrap'

console.log('Hiphop mode ON!');
// 如果需要初始化特定的 Bootstrap 元件可以在這裡寫
// 例如：初始化所有的 Tooltip
document.addEventListener('DOMContentLoaded', () => {
  console.log('時光書屋已載入');
});