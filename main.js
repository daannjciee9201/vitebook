import './style.scss'  // 這行沒問題
import 'bootstrap/dist/css/bootstrap.min.css' // 加入這行確保基礎樣式必載入
import 'bootstrap'

console.log('Hiphop mode ON!');

document.addEventListener('DOMContentLoaded', () => {
    // 這裡可以寫之後的互動功能
});